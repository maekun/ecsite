spring:
  mvc:
    view:
      prefix:  /WEB-INF/views/
      suffix: .jsp
      
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ecsite
    username: postgres
    password: postgres
      
server:
  context-path: /ec

security:
  basic:
    enabled: false